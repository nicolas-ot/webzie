import styles from './cart.module.scss';

import Layout from '../../hoc/Layout/Layout';

const Cart = () => {
  return (
    <Layout>
      <div>
        <div>cart</div>
      </div>
    </Layout>
  );
};

export default Cart;
